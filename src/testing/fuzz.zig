// serval-15q
//! Fuzz harness entry points. Reserved: wire into `zig build test --fuzz`
//! once the schema-driven decoder exists (decoders must never crash on
//! arbitrary input — they return DecodeError).
